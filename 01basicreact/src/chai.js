function Chai(){//this name should be in Capital letter start like "Chai"
    //in basic react code will run but no output but in vit engine it will through error
    return (
        <>
        <h1>Chai in Code</h1>
        </>
    )
}
export default Chai